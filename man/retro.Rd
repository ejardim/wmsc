% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retro_new.R
\name{retro}
\alias{retro}
\title{Retrospective Analysis for FLa4a Assessments}
\usage{
retro(stk, idxs, fit, peels = 5, kfrac = 0.3)
}
\arguments{
\item{stk}{An \code{FLStock} object containing the base stock data.}

\item{idxs}{An \code{FLIndices} object containing tuning indices.}

\item{fit}{An \code{a4aFitSA} object representing the base assessment fit.}

\item{peels}{Integer. The number of retrospective years to peel off. Default is 5.}

\item{kfrac}{Numeric. Fractional penalty applied to reduce knots in \code{s(year, ...)} terms for each year removed. Default is 0.3.}
}
\value{
An \code{FLStocks} object containing the refitted stock for the base run and each peel, named by terminal year.
}
\description{
Runs a retrospective analysis on an existing \code{FLa4a} assessment fit.
It automatically extracts submodels from the base fit, truncates the data sequentially,
and refits the models. Knots (\code{k}) in \code{s(year, ...)} splines are dynamically reduced
to avoid overparameterization on shortened time series, for fmodel and srmodel.
}
\examples{
\dontrun{
data(ple4)
data(ple4.index)
idxs <- FLIndices(BTS = ple4.index)
fit <- sca(ple4, idxs)
retros <- retro(stk = ple4, idxs = idxs, fit = fit, peels = 5)
}
}
